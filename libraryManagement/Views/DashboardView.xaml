<UserControl x:Class="libraryManagement.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:libraryManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Padding="20">

    <UserControl.DataContext>
        <viewmodels:DashboardViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F5F6FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,20">
            <Border Background="#3498DB" CornerRadius="8" Margin="0,0,10,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="TỔNG SÁCH TRONG KHO" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalBooksCount}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#2ECC71" CornerRadius="8" Margin="10,0,10,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="ĐỘC GIẢ ĐĂNG KÝ" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalReadersCount}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#E67E22" CornerRadius="8" Margin="10,0,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="SÁCH ĐANG ĐƯỢC MƯỢN" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ActiveBorrowsCount}" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="⚠️ CẢNH BÁO: SÁCH SẮP HẾT TRONG KHO" FontSize="16" FontWeight="Bold" Foreground="#C0392B" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding LowStockBooks}" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="30" Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Sách" Binding="{Binding BookId}" Width="100"/>
                        <DataGridTextColumn Header="Tên Sách" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Tác Giả" Binding="{Binding Author}" Width="200"/>
                        <DataGridTextColumn Header="Còn Lại" Binding="{Binding AvailableQuantity}" Width="120"/>
                        <DataGridTextColumn Header="Tổng Quy Định" Binding="{Binding TotalQuantity}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>