<UserControl x:Class="libraryManagement.Views.BorrowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:libraryManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Padding="20">

    <UserControl.DataContext>
        <viewmodels:BorrowViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F5F6FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,15,0">
            <StackPanel>
                <TextBlock Text="ĐĂNG KÝ MƯỢN SÁCH" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <TextBlock Text="Mã Độc Giả:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding ReaderId, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Tên Độc Giả:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding ReaderName, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Chọn Sách Mượn:" Margin="0,5,0,2"/>
                <ComboBox ItemsSource="{Binding AvailableBooks}" SelectedItem="{Binding SelectedFormBook}" DisplayMemberPath="Title" Height="30" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBlock Text="Số lượng còn trong kho: " Foreground="Gray"/>
                    <TextBlock d:Text="0" Text="{Binding SelectedFormBook.AvailableQuantity}" FontWeight="Bold" Foreground="#2980B9"/>
                </StackPanel>

                <Button Content="Cho Mượn Sách" Command="{Binding BorrowBookCommand}" Height="35" Background="#3498DB" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,5,0,5"/>
                <Button Content="Xử Lý Trả Sách" Command="{Binding ReturnBookCommand}" Height="35" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Tickets}" SelectedItem="{Binding SelectedTicket}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" RowHeight="30" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding TicketId}" Width="70"/>
                    <DataGridTextColumn Header="Mã ĐG" Binding="{Binding ReaderId}" Width="70"/>
                    <DataGridTextColumn Header="Tên Độc Giả" Binding="{Binding ReaderName}" Width="130"/>
                    <DataGridTextColumn Header="Tên Sách" Binding="{Binding BookTitle}" Width="*"/>
                    <DataGridTextColumn Header="Ngày Mượn" Binding="{Binding BorrowDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="90"/>
                    <DataGridTextColumn Header="Hạn Trả" Binding="{Binding DueDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="90"/>
                    <DataGridTextColumn Header="Ngày Trả" Binding="{Binding ReturnDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="90"/>
                    <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>