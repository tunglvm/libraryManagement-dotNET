<UserControl x:Class="libraryManagement.Views.ReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:libraryManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Padding="20">

    <UserControl.DataContext>
        <viewmodels:ReaderViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F5F6FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,15,0">
            <StackPanel>
                <TextBlock Text="QUẢN LÝ ĐỘC GIẢ" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <TextBlock Text="Mã Thẻ Độc Giả:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding ReaderId, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Họ Và Tên:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="Số Điện Thoại:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

                <Button Content="Thêm Độc Giả" Command="{Binding AddReaderCommand}" Height="35" Background="#2ECC71" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="0,5,0,5"/>
                <Button Content="Xóa Độc Giả" Command="{Binding DeleteReaderCommand}" Height="35" Background="#E74C3C" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Readers}" SelectedItem="{Binding SelectedReader}" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="30" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Độc Giả" Binding="{Binding ReaderId}" Width="120"/>
                    <DataGridTextColumn Header="Họ Và Tên" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding PhoneNumber}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>